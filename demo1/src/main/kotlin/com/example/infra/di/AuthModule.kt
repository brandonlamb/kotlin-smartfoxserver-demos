package com.example.infra.di

/*
import com.example.auth.domain.api.AuthRepository
import com.example.infra.basic.AuthRepo
import com.google.inject.AbstractModule

class AuthModule : AbstractModule() {
  override fun configure() {
    bind(AuthRepository::class.java).to(AuthRepo::class.java).asEagerSingleton()
  }
}
*/
